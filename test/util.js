const expect = require('chai').expect;


module.exports = {
    // haveProperties: (model_instance, prop_list) => {  // todo not using this yet
    //     prop_list.forEach(prop => {
    //         expect(model_instance).to.have.property(prop);
    //         }
    //     );
    // },
};

